# 复盘 #13 — v0.8.6 IP 访问控制与 SEO 宣传清理（2026-08-26）

> ↩ 复盘索引：[README](README.md)
>
> 当前状态：主站 v0.8.6、运营后台 v0.4.19、API v0.4.11 已于 2026-08-26 上线；白名单限制模式已开启，首条规则为当前网络白名单，黑名单初始为空。

## 数据快照与问题边界

- 域名风险要求公开入口能立即收紧到指定 IP，同时保留不受限制的后台恢复路径。
- 单表唯一地址约束消除了白名单和黑名单同时命中的冲突；规则顺序固定为黑名单拒绝、白名单模式判断、放行。
- 当前后台请求 IP 自动加入白名单；直接删除被拒绝，移入黑名单必须强确认，恢复访问使用同一行原子切换。
- 本次只清除 HTML、robots、sitemap 和 OG 资产中的 SEO 宣传层。首页平台能力文案、QQ 引导、下载路径、错误提示、埋点、README 和研发文档均未删除。
- 访问控制落在 nginx access phase。生产宝塔 nginx 未编译 `auth_request`，最终使用其内置 ngx_http_lua 通过 cosocket 调用内部判定 API；已完整加载到浏览器内的旧页面仍只能在刷新或再次请求资源时被拦截。

## 实现与本地验收

- API 新增 `site_access_restricted` 和 `site_access_ip_rules`，精确解析并规范化 IPv4/IPv6，拒绝 CIDR、主机名、端口、zone identifier 与回环规则配置。
- 内部判定只读取 nginx 覆盖的 `X-Real-IP`；黑名单始终拒绝，存储异常显式返回 500，回环地址仅供服务器内部验收隐式放行。
- 后台配置中心增加独立访问控制卡片，覆盖双列表、备注、原子移动、当前 IP 强确认、恢复访问、失败保留服务端状态与重试。
- nginx 配置豁免 `/admin/`、`/api/admin/`、`/api/health` 与证书验证路径，统一限制主站、静态资源、下载和公开 API，并为页面/API 分别返回中性 HTML/JSON 403。
- 部署流水线改为先按 `.deploy-manifest.json` 校验服务器目录全部文件，再用服务器回环验 nginx；公网 smoke 根据内部判定接受 200 或预期 403，回滚复用相同逻辑。
- `index.html` 只保留通用标题与 `noindex, nofollow, noarchive`，robots 全站禁止；sitemap 和两份 OG 图片资产已删除。

| 验证项 | 结果 |
|---|---|
| IP 访问控制自动测试 | 8/8 通过：规范化、默认值、持久化、鉴权、非法 payload、唯一地址、当前 IP 保护、判定优先级、仅信任 `X-Real-IP` 与异常 500 |
| 既有配置回归 | API feature flag 6/6、主站 public config 4/4 通过 |
| 既有专项回归 | XM 11 通过/1 黄金样本跳过；封面 16 通过/1 私有样本跳过；浏览器兼容 7/7；overview 3/3；M4A 私有样本 1 跳过 |
| 三端构建 | 主站 v0.8.6、后台 v0.4.19、API v0.4.11 全部通过；后台既有大 chunk 警告保留 |
| 定向 lint | 本次新增/修改 TypeScript 文件 0 问题 |
| SEO 构建断言 | 无 canonical、description、keywords、JSON-LD、OG/Twitter、sitemap 或 OG 图片；robots/meta/受限页均阻止索引 |
| 产品边界断言 | 首页平台文案、QQ 入口、酷狗失败提示与 `/downloads/qq-music-v19.51-windows.zip` 路径仍在产物中 |
| API 重启持久化 | 限制模式与 allow/deny/备注重启后保持；白名单 204、黑名单 403、未配置 403、回环 204 |
| 后台浏览器交互 | 当前 IP 自动加入、拉黑强确认、限制模式失败保护、恢复、备注编辑、移动、删除均通过 |
| 桌面与窄屏 | 1280×720 与 390×667 通过；窄屏 `innerWidth = scrollWidth = 390`，表格只在卡片内横向滚动 |
| CI 静态校验 | workflow YAML、shell/Node 语法通过；本地清单 25 个文件大小与 SHA-256 全量一致 |

本地实现阶段本机没有 nginx 可执行文件，因此当时没有声称完成 nginx 运行验收；发布阶段已在生产服务器上对最终配置执行 `nginx -t` 并 reload，结果见下节。

## 生产发布证据

- 功能 PR [#62](https://github.com/nohy1053885871-afk/musiczh/pull/62) 合并为 `4e3643b1e097`。首次 Actions run `32917802614` 已上传主站与后台，但 GitHub Runner 无法解析被封域名，公网 smoke 失败；流水线按设计完成回滚，生产未停留在半发布状态。
- CI 兼容修复 PR [#63](https://github.com/nohy1053885871-afk/musiczh/pull/63) 合并为 `cfc0d4766c5b`：只有 DNS 解析失败可跳过公网探测，其他 curl 错误仍失败；服务器目录清单、SHA-256 和回环资源验收继续强制执行。
- 前端 Actions run `32918201433` 成功：主站与后台部署、目录全量哈希和服务器回环检查全部通过，API 按预期 skipped。手动 API run `32918326481` 成功：依赖安装、pm2 重启与内网健康检查通过。
- nginx 首次套用 `auth_request` 时由 `nginx -t` 明确拒绝，脚本自动恢复备份 `/www/backup/musiczh/nginx/sleepno.cn.conf.20260826-092209`，未 reload 错误配置。随后试验 `ngx.location.capture` 虽通过语法检查，但旧 Lua 模块不支持 HTTP/2 子请求并对公开请求返回 500；该方案未保留。
- 最终采用 `access_by_lua_block` + `ngx.socket.tcp()` 以 HTTP/1.1 请求 `127.0.0.1:8787/internal/site-access-check`，1 秒连接/发送/读取超时，非 204/403 一律 500 fail-closed。最终配置备份为 `/www/backup/musiczh/nginx/sleepno.cn.conf.20260826-093941`，reload 前后 `nginx -t` 均成功。
- 白名单限制模式已开启，当前网络规则为 `allow`，上线时规则计数为允许 1、禁止 0。仓库与复盘不记录具体公网 IP。
- 生产矩阵通过：当前白名单网络的主站、公开 API、静态资源、下载、后台、健康检查均为 200；非白名单来源的主站、公开 API、静态资源与下载均为 403，后台和健康检查仍为 200；API 拒绝体严格为 `{"error":"access_restricted"}`，页面拒绝体包含“访问受限”。服务器回环主站为 200；内部判定对白名单为 204、未知地址为 403。
- robots 返回全站 `Disallow: /`；主站响应头为 `X-Robots-Tag: noindex, nofollow, noarchive`。最终 Lua 配置启用后 nginx 错误日志未出现新的鉴权错误。

## 上次 Action 回顾

- [ ] v0.8.2、v0.8.3、v0.8.4 的生产观测仍待回填，本次迭代不补造历史数据。
- [ ] v0.8.5 的 24 小时与 7 天指标仍待回填。
- [ ] 低版本浏览器支持门槛复核仍绑定下次 Vite/Worker 架构升级。

## 上线观测与新增 Action Items

| 验证什么 | 事件 / 信号 | 期望 | 窗口 |
|---|---|---|---|
| 访问判定稳定 | nginx 200/403 分布 | 与白名单/黑名单配置一致，无异常激增 | 上线后 1 小时、24 小时 |
| 鉴权服务可用 | `/internal/site-access-check` 5xx、pm2 错误日志 | 5xx 为 0，无 SQLite/路由异常 | 上线当次、1 小时、24 小时 |
| 恢复路径可靠 | `/admin/`、`/api/admin/`、`/api/health` | 当前 IP 拉黑后仍为 200，可恢复访问 | 上线当次 |
| 部署限制感知 | user deploy/rollback smoke | 白名单 Runner 为 200；未配置或黑名单 Runner 为预期 403，均不误回滚 | 首次受限部署 |
| 搜索索引停止 | robots、HTML meta、`X-Robots-Tag` | 三层均保持 noindex/noarchive | 上线当次、24 小时 |

- [x] 项目主已明确授权上线。
- [x] 生产 nginx 配置已备份；最终配置仅在 `nginx -t` 成功后 reload。
- [x] 已部署三端、安装 nginx 鉴权、加入当前 IP 白名单并开启限制模式；上线时黑名单为空，完整正反向访问矩阵通过。
- [x] 首次受限部署的服务器清单与回环 smoke 通过；域名 DNS 不可解析时不再误回滚，其他网络错误仍保持失败。
- [ ] 三端标签在发布收尾合并后创建；1 小时与 24 小时观测数据按窗口继续回填。
